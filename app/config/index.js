var config = {
	port: 3001,

	database: {
	    host: 'localhost',
	    database: 'turf',
	    user: 'root',
	    password: ''
	},

	redis: {
		host: '127.0.0.1',
		port: 6379
	}
};

module.exports = config;